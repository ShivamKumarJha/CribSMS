<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CribSMS" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- EditText styles -->
    <style name="Theme.App.TextInputLayout.Normal" parent="Widget.MaterialComponents.TextInputLayout.FilledBox.Dense">
        <item name="android:textColor">@color/text</item>
        <item name="android:textColorHint">@color/grey_500</item>
        <item name="boxStrokeColor">@color/grey_200</item>
        <item name="hintTextColor">@color/grey_500</item>
        <item name="background">@color/dialog</item>
        <item name="boxBackgroundColor">@color/dialog</item>
    </style>

    <style name="Theme.App.EditText.Normal" parent="Widget.AppCompat.EditText">
        <!-- empty for now -->
    </style>

    <!-- Button styles -->
    <style name="Theme.GroupAlike.Button.Normal" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/selector_button</item>
        <item name="textAllCaps">false</item>
        <item name="android:padding">12dp</item>
    </style>

</resources>